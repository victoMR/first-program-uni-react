import React from "react";
import ReactDOM from "react-dom/client";
import { Newsok } from "./components/news";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <>
    <Newsok />
  </>
);
